/* ===== Admin Manage — 数据管理 ===== */

.admin-entry-grid {
  grid-template-columns: repeat(2, minmax(0, 1fr));
  max-width: 620rpx;
}

/* 确保两个入口按钮大小一致 */
.admin-entry-grid .qf-entry-card {
  min-height: 156rpx;
  height: 156rpx;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
}

/* ---- Shared Form Styles ---- */
.import-card {
  max-width: 660rpx;
  margin-left: auto;
  margin-right: auto;
}

.form-group {
  margin-bottom: 24rpx;
}

.form-label {
  display: block;
  margin-bottom: 10rpx;
  font-size: 23rpx;
  font-weight: 650;
  color: #173a32;
}

.required {
  color: #ef4444;
  font-weight: 700;
}

.optional {
  color: #8a9d92;
  font-weight: 400;
  font-size: 20rpx;
}

.import-picker {
  margin-top: 0;
  border-width: 2rpx;
  border-color: rgba(23, 58, 50, 0.26);
  background: rgba(255, 255, 255, 0.82);
  min-height: 80rpx;
}

/* ---- Form Row (2-column) ---- */
.form-row {
  display: flex;
  gap: 20rpx;
}

.form-half {
  flex: 1;
  min-width: 0;
}

/* ---- Form Inputs ---- */
.form-input {
  margin-top: 0;
  min-height: 80rpx;
  padding: 0 20rpx;
  border-width: 2rpx;
  border-color: rgba(23, 58, 50, 0.26);
  background: rgba(255, 255, 255, 0.82);
  font-size: 25rpx;
  border-radius: 22rpx;
}

.form-textarea {
  margin-top: 0;
  min-height: 140rpx;
  padding: 18rpx 20rpx;
  border-width: 2rpx;
  border-color: rgba(23, 58, 50, 0.26);
  background: rgba(255, 255, 255, 0.82);
  font-size: 25rpx;
  border-radius: 22rpx;
  line-height: 1.5;
}

/* ---- Selected preview cards ---- */
.selected-preview {
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 22rpx;
  margin-bottom: 8rpx;
  border-radius: 20rpx;
  background: rgba(255, 255, 255, 0.52);
  border: 1rpx solid rgba(83, 117, 91, 0.14);
}

.selected-preview.course-preview {
  margin-bottom: 24rpx;
}

.preview-avatar {
  width: 66rpx;
  height: 66rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.preview-avatar.student-avatar-bg {
  background: linear-gradient(145deg, #0d9488, #0f766e);
}

.preview-avatar.course-avatar-bg {
  background: linear-gradient(145deg, #f97316, #ea580c);
}

.preview-avatar-text {
  color: #fff;
  font-size: 26rpx;
  font-weight: 700;
}

.preview-info {
  flex: 1;
  min-width: 0;
}

.preview-name {
  display: block;
  font-size: 26rpx;
  font-weight: 650;
  color: #173a32;
}

.preview-meta {
  display: block;
  margin-top: 4rpx;
  font-size: 21rpx;
  color: #65756b;
}

.preview-courses {
  display: block;
  margin-top: 4rpx;
  font-size: 20rpx;
  color: #0d9488;
  font-weight: 600;
}

/* ---- Import arrow ---- */
.import-arrow {
  display: flex;
  justify-content: center;
  padding: 12rpx 0;
  margin-bottom: 8rpx;
}

.arrow-down {
  font-size: 36rpx;
  color: #f97316;
  animation: bounceDown 800ms ease-in-out infinite;
}

@keyframes bounceDown {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(6rpx); }
}

/* ---- Import button ---- */
.import-btn {
  margin-top: 28rpx;
  min-height: 88rpx;
  font-size: 28rpx;
  border-radius: 26rpx;
}

.import-btn[disabled] {
  opacity: 0.55;
}

/* ---- Result banner ---- */
.result-banner {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-top: 24rpx;
  padding: 20rpx 24rpx;
  border-radius: 18rpx;
  animation: fadeSlideIn 200ms ease-out both;
}

.result-banner.success {
  background: rgba(22, 163, 74, 0.08);
  border: 1rpx solid rgba(22, 163, 74, 0.2);
}

.result-banner.error {
  background: rgba(220, 38, 38, 0.06);
  border: 1rpx solid rgba(220, 38, 38, 0.18);
}

.result-icon {
  font-size: 28rpx;
}

.result-text {
  font-size: 23rpx;
  font-weight: 600;
  color: #173a32;
  line-height: 1.4;
}

/* ---- Help card ---- */
.help-card {
  max-width: 660rpx;
  margin-left: auto;
  margin-right: auto;
  margin-top: 28rpx;
}

.help-title {
  display: block;
  font-size: 24rpx;
  font-weight: 650;
  color: #173a32;
  margin-bottom: 14rpx;
}

.help-text {
  display: block;
  font-size: 21rpx;
  color: #65756b;
  line-height: 1.7;
}

/* ---- Recent Courses ---- */
.recent-courses-card {
  max-width: 660rpx;
  margin-left: auto;
  margin-right: auto;
}

.recent-course-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  padding: 18rpx 0;
  border-bottom: 1rpx solid rgba(83, 117, 91, 0.1);
}

.recent-course-row:last-child {
  border-bottom: 0;
}

.rc-left {
  flex: 1;
  min-width: 0;
}

.rc-name {
  display: block;
  font-size: 25rpx;
  font-weight: 650;
  color: #173a32;
}

.rc-meta {
  display: block;
  margin-top: 4rpx;
  font-size: 20rpx;
  color: #65756b;
}

/* ===== Animations ===== */
@keyframes fadeSlideIn {
  from {
    opacity: 0;
    transform: translateY(-8rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* ===== Panel ===== */
.qf-panel {
  max-width: 660rpx;
  margin-left: auto;
  margin-right: auto;
}

/* Light paper management overrides */
.form-label,
.preview-name,
.result-text,
.help-title,
.rc-name {
  color: #10224A;
}

.optional,
.preview-meta,
.help-text,
.rc-meta {
  color: #8A8D88;
}

.import-picker,
.form-input,
.form-textarea,
.selected-preview {
  border-color: rgba(16, 34, 74, 0.1);
  background: #FFFCF2;
}

.selected-preview {
  background: #F6F3E8;
}

.preview-avatar.student-avatar-bg {
  background: #EEF5DD;
}

.preview-avatar.course-avatar-bg {
  background: #FFE8BF;
}

.preview-avatar-text {
  color: #10224A;
}

.preview-courses,
.result-banner.success .result-text {
  color: #3E8B68;
}

.arrow-down {
  color: #F59A23;
}

.result-banner.success {
  background: #EEF5DD;
  border-color: rgba(62, 139, 104, 0.18);
}

.result-banner.error {
  background: rgba(255, 232, 224, 0.86);
  border-color: rgba(163, 58, 58, 0.16);
}

.recent-course-row {
  border-bottom-color: rgba(16, 34, 74, 0.08);
}

/* ---- Enrollment sync ---- */
.sync-section-title {
  margin-top: 28rpx;
}

.enrollment-sync-card {
  max-width: 660rpx;
  margin-left: auto;
  margin-right: auto;
}

.sync-action-row {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 12rpx;
  margin-bottom: 22rpx;
  padding: 8rpx;
  border-radius: 22rpx;
  background: #F6F3E8;
  border: 1rpx solid rgba(16, 34, 74, 0.08);
}

.sync-action {
  min-height: 66rpx;
  padding: 0 10rpx;
  border-radius: 18rpx;
  background: transparent;
  color: #5F6470;
  font-size: 24rpx;
  font-weight: 650;
  line-height: 66rpx;
}

.sync-action::after {
  border: 0;
}

.sync-action.active {
  background: #10224A;
  color: #FFFDF6;
  box-shadow: 0 10rpx 22rpx rgba(16, 34, 74, 0.16);
}

.sync-student-line {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
  margin-bottom: 22rpx;
  padding: 18rpx 20rpx;
  border-radius: 18rpx;
  background: #EEF5DD;
  border: 1rpx solid rgba(62, 139, 104, 0.14);
}

.sync-label {
  color: #5F6470;
  font-size: 22rpx;
  font-weight: 650;
}

.sync-value {
  flex: 1;
  min-width: 0;
  color: #10224A;
  font-size: 24rpx;
  font-weight: 700;
  text-align: right;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.sync-submit {
  margin-top: 18rpx;
}