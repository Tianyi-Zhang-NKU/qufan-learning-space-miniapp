<view class="qf-role-tabbar {{fixed ? '' : 'inline'}}">
  <view
    wx:for="{{list}}"
    wx:key="url"
    class="qf-role-tab {{item.primary ? 'primary' : ''}} {{item.active ? 'active' : ''}}"
    data-url="{{item.url}}"
    bindtap="go"
  >
    <view class="qf-role-mark">
      <image class="qf-role-icon" src="{{item.icon}}" mode="aspectFit"></image>
    </view>
    <text class="qf-role-text">{{item.text}}</text>
  </view>
</view>
