# 2026-07-12 验收录屏差距审计

录屏来源：`D:\Desktop\小程序验收.mp4`。需求原文整理见 `docs/client-meeting-requirements-2026-07-12.md`。

## 本地实现状态

| 编号 | 甲方要求 | 当前状态 | 验证证据 | 剩余条件 |
| --- | --- | --- | --- | --- |
| P0-1 | 课堂小测和本讲总结分别进入正确内容 | 已完成 | 总结页仅筛选 `post`；小测和总结入口分别进入对应页面；`smoke-test` 覆盖 | 需微信开发者工具/真机复验点击路径 |
| P0-2 | 首页突出下一次上课，收起冗余排课行，提示课次切换 | 已完成 | 首页使用 `next-session-panel`；不再渲染 `scheduleLines`；小测和总结提供前后切换、页码和稳定圆点窗口 | 需 375px 模拟器截图复验 |
| P0-3 | 教师只保留小测、总结；通关独立 | 已完成 | 教师反馈页仅有 `pre`、`post`；待办直达独立通关确认 | 需真机操作复验 |
| P0-4 | 教师待办优先并有未处理数量 | 已完成 | 教师首页待办位于今日课程之前，使用稳定待办徽标；确认后待办自动消失 | 需真机操作复验 |
| P0-5 | 教师可见班级通关率 | 已完成 | 课程集合和领域 API 返回 `classPassRate`、`classConfirmedPasses` | 需真实同步数据校验口径 |
| P0-6 | 教研统一维护资料，教师端隐藏复杂上传 | 已完成（本地） | `research-admin` 支持原始 PDF/Word 文件关联、资料包、题目单元、版本、发布与平行班绑定；教师上传页不再注册 | 真实文件存储和首期资料导入待定 |
| P0-7 | 教务只读同步 | 已完成（本地边界） | 课程、人员、课次、教室和成员写接口均拒绝 `READ_ONLY_ENROLLMENT`；已提供同步契约 | 等待甲方教务 API、签名和测试环境 |
| P0-8 | 管理端通关看板、重点关注学员 | 已完成 | 年级/学科/教师通关率、关注与优秀学员、备注均已实现并带权限过滤 | 等待真实数据验证阈值 |
| P0-9 | 手机号多身份、超级管理员分级授权 | 已完成（Mock） | `users`、`userRoles`、`adminGrants`；登录页可选择身份，个人页可切换身份，年级+学科范围与超级管理员授权已验证 | 等待微信手机号授权映射 |
| P1-1 | 正式 Logo、真实头像 | 部分完成 | 默认姓氏头像与正式登录层级已实现 | 正式 Logo、微信头像授权/上传接口未提供 |
| P1-2 | 自动重点关注与备注 | 已完成 | 连续未通关、低通关率、优秀阈值和管理员备注已覆盖 | 甲方确认最终阈值 |
| P1-3 | 移除课程改名、教师直播、教室合集/全校课表 | 已完成 | 教师主路径已移除改名和直播；教室课表与教师题目上传页均不再注册 | 旧源码仅保留为历史记录，不属于可访问产品路径 |
| P2-1 | ClassIn 按课程/老师/课节 ID 对接 | 已完成（Mock/契约） | 每课节三元映射、换教室稳定、未配置拒绝、10 分钟有效期已覆盖 | 等待 ClassIn 签名规范和测试账号 |
| P2-2 | 真实 PDF/Word 错题本 | 接口模型已完成 | 本地生成可打印导出响应模型 | 等待云存储和文件生成服务 |

## 当前验证

已通过：

```powershell
node scripts/check-js.js
node scripts/smoke-test.js
git diff --check
```

`smoke-test` 当前覆盖：登录身份选择、角色范围隔离、超级管理员授权入口、通关统计、重点关注备注、资料包原始文件关联与版本绑定、教师待办闭环、学生反馈分流、课次导航、教务写入拒绝、ClassIn 三元映射和短期入口。

## 上线前阻塞项

1. CloudBase 环境、云函数和私有存储尚未部署。
2. 微信手机号授权与用户身份映射尚未接入。
3. 教务同步接口、签名密钥和测试环境尚未提供。
4. ClassIn 签名 API、三元 ID 字段样例和测试账号尚未提供。
5. 正式 Logo、头像策略、首期试点范围与教研资料规范待甲方确认。
6. 当前机器未安装微信开发者工具 CLI，尚未生成预览码或完成 375px/真机截图验收。
